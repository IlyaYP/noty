package logging

const (
	// CorrelationIDKey defines the logging key for tracking the Correlation ID.
	CorrelationIDKey = "correlation_id"

	// ServiceKey defines the logging key for tracking the service.
	ServiceKey = "service"

	// SendingIDKey defines the logging key for tracking the Order ID.
	SendingIDKey = "sending_id"

	// ClientIDKey defines the logging key for tracking the User.
	ClientIDKey = "client_id"

	// IDKey defines logging key to track object ID.
	IDKey = "id"
)
